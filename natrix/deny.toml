[graph]
targets = [
    "wasm32-unknown-unknown",
]
all-features = true

[advisories]
ignore = [
    { id="RUSTSEC-2024-0436", reason="Used by lightningcss, so we might as well use it as well."}
]

[licenses]
allow = [
    "MIT",
    "Zlib",
    "Unicode-3.0",
    "BSL-1.0",
]

[bans]
multiple-versions = "deny"
wildcards = "deny"
